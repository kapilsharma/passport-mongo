//Trying to finish users service for service laer and redirect all db task through service layer.
// Not complete and usable right now.
// TODO: figure out how to add service layer in express.

var User = require('../models/user');

module.exports = function(passport){

	}